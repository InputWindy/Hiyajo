#pragma once

/**
 * Render extension. FRenderSystem (extension + render thread) lives in
 * Render/RenderSystem.h; this header is kept as the historical include path.
 */

#include <Render/RenderSystem.h>
