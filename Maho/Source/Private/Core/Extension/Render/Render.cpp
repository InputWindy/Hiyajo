// FRenderSystem implementation now lives in Render/RenderServer.cpp
// (the extension and render-thread worker were merged). This translation
// unit is intentionally empty; kept so the source glob stays stable.
#include <Core/Extension/Render/Render.h>
