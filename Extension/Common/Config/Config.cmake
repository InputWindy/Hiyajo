# Config plugin: INI parser (sections + key=value, no third-party).
# The DLL target is built by codegen; cross-plugin deps go through
# .cplugin Dependencies → CMake include dirs + add_dependencies automatically.

