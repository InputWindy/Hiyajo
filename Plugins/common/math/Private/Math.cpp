// Math is a header-only pure library — a noexcept static API. No state, no
// lifecycle, no factory. The DLL target exists only so dependents can propagate
// the glm link through Math PUBLIC.
#include "MathTool.h"
