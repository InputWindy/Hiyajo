# Generated by Maho CreateProject.
cmake_minimum_required(VERSION 3.20)
project(ExampleEngine LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# MSVC: source files are UTF-8 (comments contain em-dash etc.).
if(MSVC)
	add_compile_options(/utf-8)
endif()

# Enable VS solution folders (FOLDER property groups targets under virtual dirs).
set_property(GLOBAL PROPERTY USE_FOLDERS ON)

# The .cproject double-click drives regeneration; stop CMake from embedding the
# top-level CMakeLists.txt into every plugin vcxproj (ZERO_CHECK auto-rerun).
set(CMAKE_SUPPRESS_REGENERATION ON)

set(ENGINE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../..")

# Third-party helpers (FetchContent reuse + mirror/proxy + Threads).
include("${ENGINE_DIR}/Build/CMake/MahoDependencies.cmake")

# Engine core headers — listed in the entry target only (the DLL tree stays clean).
file(GLOB MAHO_HEADERS CONFIGURE_DEPENDS "${ENGINE_DIR}/Source/Public/**/*.h")

# Engine core private sources — only the entry needs them (Maho::Main uses
# FAssembly + Fatal); the plugin DLL only uses the header-only core.
file(GLOB MAHO_PRIVATE CONFIGURE_DEPENDS "${ENGINE_DIR}/Source/Private/**/*.cpp")

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Common/Archive")
include("${ENGINE_DIR}/Plugins/Common/Archive/Archive.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Common/Compress")
include("${ENGINE_DIR}/Plugins/Common/Compress/Compress.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Common/ConsoleVariable")
include("${ENGINE_DIR}/Plugins/Common/ConsoleVariable/ConsoleVariable.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Config")
include("${ENGINE_DIR}/Plugins/Engine/Core/Config/Config.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Exception")
include("${ENGINE_DIR}/Plugins/Engine/Core/Exception/Exception.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Log")
include("${ENGINE_DIR}/Plugins/Engine/Core/Log/Log.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Name")
include("${ENGINE_DIR}/Plugins/Engine/Core/Name/Name.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Paths")
include("${ENGINE_DIR}/Plugins/Engine/Core/Paths/Paths.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/Platform")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/Platform/Platform.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RHI")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RHI/RHI.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Common/Reflect")
include("${ENGINE_DIR}/Plugins/Common/Reflect/Reflect.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/Resource")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/Resource/Resource.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/Asset")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/Asset/Asset.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/Script")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/Script/Script.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Text")
include("${ENGINE_DIR}/Plugins/Engine/Core/Text/Text.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/Core/Timer")
include("${ENGINE_DIR}/Plugins/Engine/Core/Timer/Timer.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Common/Unicode")
include("${ENGINE_DIR}/Plugins/Common/Unicode/Unicode.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/Render")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/Render/Render.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Scene")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Scene/Scene.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/DrawTriangleFeature")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/DrawTriangleFeature/DrawTriangleFeature.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Frame")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Frame/Frame.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/UIFeature")
include("${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/UIFeature/UIFeature.cmake")
unset(_MOD_PLUGIN_DIR)

set(_MOD_PLUGIN_DIR "${CMAKE_CURRENT_SOURCE_DIR}/Plugins/ExampleEditor/ExampleEditor")
include("${CMAKE_CURRENT_SOURCE_DIR}/Plugins/ExampleEditor/ExampleEditor/ExampleEditor.cmake")
unset(_MOD_PLUGIN_DIR)

# The project root — the entry plugin (under Plugins/, Public headers + Private impl).
file(GLOB ExampleEngine_PUBLIC_HEADERS CONFIGURE_DEPENDS "Plugins/ExampleEngine/Public/*.h")
file(GLOB ExampleEngine_PRIVATE_HEADERS CONFIGURE_DEPENDS "Plugins/ExampleEngine/Private/*.h")
file(GLOB ExampleEngine_PRIVATE_SOURCES CONFIGURE_DEPENDS "Plugins/ExampleEngine/Private/*.cpp")
add_library(ExampleEngine SHARED
	${ExampleEngine_PUBLIC_HEADERS}
	${ExampleEngine_PRIVATE_HEADERS}
	${ExampleEngine_PRIVATE_SOURCES}
	Plugins/ExampleEngine/ExampleEngine.cplugin
	Plugins/ExampleEngine/ExampleEngine.cmake
	Plugins/ExampleEngine/settings.json
	Plugins/ExampleEngine/ExampleEngine.md
	Plugins/ExampleEngine/API.md
)
source_group(TREE "${CMAKE_CURRENT_SOURCE_DIR}/Plugins/ExampleEngine" FILES
	${ExampleEngine_PUBLIC_HEADERS}
	${ExampleEngine_PRIVATE_HEADERS}
	${ExampleEngine_PRIVATE_SOURCES}
)
set_source_files_properties(Plugins/ExampleEngine/ExampleEngine.cplugin, Plugins/ExampleEngine/ExampleEngine.cmake, Plugins/ExampleEngine/settings.json, Plugins/ExampleEngine/ExampleEngine.md, Plugins/ExampleEngine/API.md PROPERTIES HEADER_FILE_ONLY ON)

target_include_directories(ExampleEngine PUBLIC
	"${ENGINE_DIR}/Source/Public"
	"${CMAKE_CURRENT_SOURCE_DIR}/Plugins/ExampleEngine/Public"
	"${ENGINE_DIR}/Plugins/Common/Archive/Public"
	"${ENGINE_DIR}/Plugins/Common/Compress/Public"
	"${ENGINE_DIR}/Plugins/Common/ConsoleVariable/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Config/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Exception/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Log/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Name/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Paths/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/Platform/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RHI/Public"
	"${ENGINE_DIR}/Plugins/Common/Reflect/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/Resource/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/Asset/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/Script/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Text/Public"
	"${ENGINE_DIR}/Plugins/Engine/Core/Timer/Public"
	"${ENGINE_DIR}/Plugins/Common/Unicode/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/Render/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Scene/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/DrawTriangleFeature/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/Frame/Public"
	"${ENGINE_DIR}/Plugins/Engine/GameEngine/RenderCore/RenderFeature/UIFeature/Public"
)
# Export the extern "C" CreateLayer() bridge — the host's single entry.
set_target_properties(ExampleEngine PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
target_compile_definitions(ExampleEngine PRIVATE MAHO_EXAMPLEENGINE_MODULE_EXPORTS)
target_link_libraries(ExampleEngine PUBLIC Maho)
add_dependencies(ExampleEngine Archive Compress ConsoleVariable Config Exception Log Name Paths Platform RHI Reflect Resource Asset Script Text Timer Unicode Render Scene DrawTriangleFeature Frame UIFeature)
target_link_libraries(ExampleEngine PUBLIC Archive Compress ConsoleVariable Config Exception Log Name Paths Platform RHI Reflect Resource Asset Script Text Timer Unicode Render Scene DrawTriangleFeature Frame UIFeature)
# Plugin DLLs co-locate with Maho.dll + EntryPoint so relative DLL deps resolve.
set_property(TARGET ExampleEngine PROPERTY RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/Binaries/$<CONFIG>")

# Cycle check — runs before every in-IDE build (host + entry depend on it).
# Prefer the engine-local venv (Setup.bat); fall back to any system python.
find_program(MAHO_PYTHON_EXECUTABLE
	NAMES python.exe python python3
	HINTS "${ENGINE_DIR}/Tools/python" "${ENGINE_DIR}/Tools/python/Scripts"
	NO_DEFAULT_PATH
)
if(NOT MAHO_PYTHON_EXECUTABLE)
	find_program(MAHO_PYTHON_EXECUTABLE NAMES python python3)
endif()
if(NOT MAHO_PYTHON_EXECUTABLE)
	message(WARNING "Maho: no python found — skipping plugin checks")
else()
	add_custom_target(MahoCheckCycle
		COMMAND "${MAHO_PYTHON_EXECUTABLE}" "${ENGINE_DIR}/Tools/check_plugin_cycle.py" "${CMAKE_CURRENT_SOURCE_DIR}/ExampleEngine.cproject"
		BYPRODUCTS "${CMAKE_CURRENT_SOURCE_DIR}/Intermediate/_cycle_check.stamp"
		VERBATIM
	)
		set_target_properties(MahoCheckCycle PROPERTIES FOLDER "ThirdParty")
		add_dependencies(ExampleEngine MahoCheckCycle)
		endif()

# The entry — code-gen boilerplate (never edited), loads ExampleEngine.dll.
# The engine source lives in the Maho library (sln folder Maho/); the exe
# imports the Maho.dll the library builds. Maho.dll is the process-unique copy
# of engine code + the shared third-party set.
add_library(Maho SHARED
	${MAHO_PRIVATE}
	${MAHO_HEADERS}
)
# activate MAHO_API → dllexport here; consumers (plugins/EntryPoint) get dllimport.
target_compile_definitions(Maho PRIVATE MAHO_BUILD_SHARED MAHO_EXPORTS)
# export every engine symbol pragmatically (fast path for stage 1; MAHO_API
# classes refine the surface later).
set_target_properties(Maho PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
# show engine source as its on-disk tree (Source/Public, Source/Private, ...)
source_group(TREE "${ENGINE_DIR}/Source" FILES ${MAHO_HEADERS} ${MAHO_PRIVATE})
target_include_directories(Maho PUBLIC "${ENGINE_DIR}/Source/Public")
# The engine third-parties are the shared infra (glm, nlohmann/json, CLI11,
# zstd …); PUBLIC so every Consumer (entry plugin + EntryPoint + sub-plugins)
# can just #include them directly.
target_link_libraries(Maho PUBLIC glm::glm nlohmann_json::nlohmann_json CLI11::CLI11 libzstd_static)
set_target_properties(Maho PROPERTIES FOLDER "Maho")

add_executable(EntryPoint WIN32 Intermediate/Main.cpp)
target_compile_definitions(EntryPoint PRIVATE MAHO_ENGINE_NAME="ExampleEngine.dll")
target_link_libraries(EntryPoint PRIVATE Maho)

# Copy the project's Config/ directory next to the binary (DefaultEngine.ini etc.
# are loaded at runtime relative to the working directory). Folded into
# EntryPoint's POST_BUILD so no standalone CopyConfig project appears in the
# solution tree - the copy runs whenever EntryPoint (re)links.
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Config")
	add_custom_command(TARGET EntryPoint POST_BUILD
		COMMAND ${CMAKE_COMMAND} -E copy_directory
			"${CMAKE_CURRENT_SOURCE_DIR}/Config"
			"${CMAKE_BINARY_DIR}/Binaries/$<CONFIG>/Config"
		COMMENT "Copying Config/ to runtime dir"
		VERBATIM
	)
endif()

# One Binaries/<Config> dir for the exe + Maho.dll + every plugin DLL, so at
# runtime they all resolve each other (both Maho.dll and the plugin DLLs must
# sit next to the EntryPoint exe). Multi-config (MSVC): per-config suffix.
set(_MAHO_BIN "${CMAKE_BINARY_DIR}/Binaries/")
foreach(_Cfg Debug Release RelWithDebInfo MinSizeRel)
	string(TOUPPER "${_Cfg}" _CfgUp)
	set_target_properties(Maho PROPERTIES "RUNTIME_OUTPUT_DIRECTORY_${_CfgUp}" "${_MAHO_BIN}/${_Cfg}")
	set_target_properties(EntryPoint PROPERTIES "RUNTIME_OUTPUT_DIRECTORY_${_CfgUp}" "${_MAHO_BIN}/${_Cfg}")
endforeach()
# A directory-level VS_DEBUGGER_WORKING_DIRECTORY is NOT propagated into the
# generated *.vcxproj (observed with the pip-installed CMake this engine uses),
# so F5 debugs with cwd = the vcxproj dir (Intermediate/) instead of
# Binaries/<Config>. But Config/ is copied to Binaries/<Config>/Config at
# post-build, so a relative "Config/..." load misses it. Apply the working
# directory as a TARGET property on the exe host instead; CMake then emits
# LocalDebuggerWorkingDirectory into EntryPoint.vcxproj.
set_target_properties(EntryPoint PROPERTIES VS_DEBUGGER_WORKING_DIRECTORY "${_MAHO_BIN}/$<CONFIG>")
# The sln startup project is EntryPoint (the app host), not ZERO_CHECK.
set_property(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" PROPERTY VS_STARTUP_PROJECT EntryPoint)
add_dependencies(EntryPoint ExampleEngine Archive Compress ConsoleVariable Config Exception Log Name Paths Platform RHI Reflect Resource Asset Script Text Timer Unicode Render Scene DrawTriangleFeature Frame UIFeature ExampleEditor MahoCheckCycle)

# Solution folders: EntryPoint at the ROOT; Maho (engine), Project, ThirdParty.
# The project root sits at Project/ itself; project plugin DLLs under
# Project/Plugins/. Engine plugins under Maho/Plugins/ — each grouped by the
# filesystem tree (shown via source_group as Public/Private on disk).
set_target_properties(ExampleEngine PROPERTIES FOLDER "Project")
source_group(TREE "${CMAKE_CURRENT_SOURCE_DIR}/Plugins/ExampleEngine" FILES
	Plugins/ExampleEngine/Public/ExampleEngine.h
	Plugins/ExampleEngine/Private/ExampleEngine.cpp
)
# EntryPoint stays at the root (no FOLDER property).
